pipeline {
    agent any

    environment {
        DOCKERHUB_USERNAME = 'tharunumesh' 
    }

    stages {
        stage('Clone Repository') {
            steps {
                git url: 'https://github.com/tharUmesh/Recipe-App.git', branch: 'main'
            }
        }

        stage('Build Docker Images') {
            steps {
                script {
                    sh 'docker compose build'
                }
            }
        }

        stage('Run Containers') {
            steps {
                script {
                    sh 'docker compose up -d'
                }
            }
        }
    }

    post {
        success {
            echo '🚀 Deployment successful!'
        }
        failure {
            echo '❌ Something went wrong.'
        }
    }
}
